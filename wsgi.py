from app import app

application = app  # Required for production server like Gunicorn
# This file is used to run the Flask application with a WSGI server.
# It allows the application to be served in a production environment.